apiVersion: v1
kind: Pod
metadata:
  name: vote-pod
  labels:
    name: vote-pod
    app: demo-voting-app
spec:
  containers:
  - name: voting-app
    image: mshaibek/voting-app_vote:1.0
    ports:
    - containerPort: 80
